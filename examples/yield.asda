Bool generator do_stuff():
    while TRUE:
        yield TRUE
        yield FALSE

let g = do_stuff()
if next[Bool](g):
    print("should get printed")
if next[Bool](g):
    print("should NOT get printed")
if next[Bool](g):
    print("should also get printed")
